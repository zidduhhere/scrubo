import 'package:flutter/material.dart';

class DynamicLocationView extends StatelessWidget {
  const DynamicLocationView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
